"use strict"; // treat all javascript code as newer version

// alert("kmato") it will be used on browser not in the node.js

console.log("kmato") //mostly developer avoid the using of semicolon in javascript 
console.log("MaTo_Khan") //because of the code readiblity and look prettiest

// 1-->primitive dataTypes
//     string = "" are used to store charactristics data it can written single quate or double quate 

//     number = are hold numeric value or float value

//     boolean = are return true of false it can be 0 or 1

//     undefined  = are just create dataType and not initialized any value that are known as the undefined datatype it will return undefined dataType

//     null = are the diffrent datatype which are contain null value and it will be return object 

//     Symbol = are used for uniqueness it will be unique and it value can not be changed once create it will return the function 

let stringName = "Kmato";
let numAge = 21;
let boolean = true;
let undefinedPlace;
let nullValue = null;
let valueSymbol = Symbol;

console.log(typeof stringName );
console.log(typeof numAge );
console.log(typeof undefinedPlace );
console.log(typeof boolean );
console.log(typeof nullValue );
console.log(typeof valueSymbol );
